import 'package:flutter/material.dart';

final jsonDecoration = BoxDecoration(
  color: Colors.white,
  border: Border.all(width: 0.5, color: Colors.grey[200]!),
);

const jsonPadding = EdgeInsets.all(5);

const jsonTextStyle = TextStyle(fontSize: 15, fontWeight: FontWeight.w500);

const jsonOffset = "          ";
